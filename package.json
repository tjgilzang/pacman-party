{
  "name": "pacman-party",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R scripts dist/ && printf '' > dist/.nojekyll"
  }
}
